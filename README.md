# ProfIS

Als erstes muss Node.js installiert sein. Zum starten muss dann erst Electron installiert werden mit "npm install --save-dev electron electron-builder" und danach kann das Program gestartet werden mit "npm run electron"
